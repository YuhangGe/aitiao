# AI 大模型配置（研发调试用）
# 复制此文件为 .env 并填入真实值：
#   cp .env.example .env
#
# .env 已被 .gitignore 忽略，不会提交到仓库。
# 在 CI 中构建时，不提供 .env 则不会预设默认值。

AITIAO_MODEL_ID=hy-vision-2.0-instruct
AITIAO_BASE_URL=https://tokenhub.tencentmaas.com/v1
AITIAO_API_KEY=sk-your-api-key-here
